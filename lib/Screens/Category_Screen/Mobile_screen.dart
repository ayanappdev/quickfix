// shoes_screen.dart
import 'package:flutter/material.dart';

class MobileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mobile'),
      ),
      body: Center(
        child: Text('Welcome to the Mobile Section!'),
      ),
    );
  }
}
