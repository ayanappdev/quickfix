// shoes_screen.dart
import 'package:flutter/material.dart';

class BeautyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Beauty Section'),
      ),
      body: Center(
        child: Text('Welcome to the Beauty Section!'),
      ),
    );
  }
}
