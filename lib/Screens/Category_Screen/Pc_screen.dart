// shoes_screen.dart
import 'package:flutter/material.dart';

class PcScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pc Section'),
      ),
      body: Center(
        child: Text('Welcome to the Pc Section!'),
      ),
    );
  }
}
